- var cdn = cdn ? cdn : ''
include includes/head1
title #{siteName}
link(rel='stylesheet', href=cdn + '/css/auto-tel.styles.css?' + version)
link(rel='stylesheet', href=cdn + '/css/basic.styles.css?' + version)
include includes/head2
#viewport
#content-loading

include includes/footer-js
script(src=cdn + '/js/proxy-js.bundle.js' + '?' + version, onload='window.et.onload()')
include includes/footer-end